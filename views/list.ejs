<!-- jshint esversion:6 -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List</title>
    <link rel="stylesheet" href="styles.css" />
</head>

<body>
    <h1 id="heading" class="box">
        <%= title%>
    </h1>
    <div class="box">
        <% items.forEach(element => { %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkboxID" value="<%= element._id %>" onchange="this.form.submit()">
                    <p>
                        <%= element.name %>
                    </p>
                </div>
                <input type="hidden" name="deleteList" value="<%= title %>">
            </form>
            <% });%>

                <form class="item" action="/" method="post">
                    <input name="item" type="text" placeholder="New Item" autocomplete="off">
                    <button type="submit" name="listname" value="<%= title %>">+</button>
                </form>
    </div>
    <footer>
        Made with 💖 by Gyanendra
    </footer>


</body>

</html>